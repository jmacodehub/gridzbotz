# ═══════════════════════════════════════════════════════════════════════════
#  🚀 GRIDZBOTZ MAINNET V1.0 — SOL/USDC PRODUCTION
#
#  Strategy: max-levels-titan (battle-tested winner)
#  Tuning: Based on 18.4h Battle Royale V6 results
#  Capital: $100-500 conservative start
#
#  Key Changes from Paper Trading:
#    ✅ Stricter fee filter (2.0x → 2.5x)
#    ✅ Wider grid spacing (0.06% → 0.18%)
#    ✅ Higher regime gate (0.02% → 0.05%)
#    ✅ Jito disabled (enable when scaling >$500)
#    ✅ Circuit breakers tightened
#    ✅ max_trade_size_usdc moved to [execution] (was silently ignored in [security])
#
#  Expected Performance (normal vol 0.1-0.5%):
#    - Fills: 10-30 per day
#    - Target ROI: +0.5-2.0% daily
#    - Max drawdown: <5%
# ═══════════════════════════════════════════════════════════════════════════

test_name = "Mainnet SOL/USDC Grid Trading V1.0"

[bot]
name            = "GridzBot-Mainnet-Titan"
version         = "5.4.0"
environment     = "production"
execution_mode  = "live"  # 🔥 LIVE TRADING
instance_id     = "mainnet-sol-usdc-v1"

[network]
cluster    = "mainnet-beta"  # 🔥 MAINNET
rpc_url    = "https://solana-mainnet.core.chainstack.com/6ab9c5a91b372ea63ecf3fd08560b281"
commitment = "confirmed"
# Backup RPC pool (add your QuickNode endpoint)
# fallback_rpc_urls = [
#     "https://YOUR-QUICKNODE-ENDPOINT.quiknode.pro/YOUR_KEY/"
# ]

[security]
# Verify file exists and permissions are correct before first live run:
#   ls -la /Users/j-mc/.config/solana/id.json
#   chmod 600 /Users/j-mc/.config/solana/id.json
wallet_path      = "/Users/j-mc/.config/solana/id.json"
require_password = false

[execution]
# Hard safety caps — whichever hits first wins
max_trade_sol          = 0.5
max_trade_size_usdc    = 50.0    # ✅ Effective cap: $50 per trade (was silently $250)

priority_fee_microlamports = 100000
max_slippage_bps           = 150
confirmation_timeout_secs  = 60
max_retries                = 3

# 🛡️ Jito MEV protection — DISABLED for initial live run
# Enable when scaling above $500 capital by uncommenting the line below.
# jito_tip_lamports = 100000    # ← uncomment to enable (~0.0001 SOL/tx tip)

[paper_trading]
initial_usdc = 200.0  # Reference only — ignored in live mode
initial_sol  = 1.0

[trading]
# 🏔️ TUNED GRID PARAMETERS (Battle Royale Winner)
grid_levels          = 20
grid_spacing_percent = 0.18
min_order_size       = 0.05
max_position_size    = 50.0

min_usdc_reserve = 20.0
min_sol_reserve  = 0.0

enable_dynamic_grid      = true
volatility_window        = 100
enable_adaptive_spacing  = true
reposition_threshold     = 0.30

enable_auto_rebalance    = true
enable_smart_rebalance   = true
rebalance_threshold_pct  = 5.0
rebalance_cooldown_secs  = 120

max_orders_per_side         = 10
order_refresh_interval_secs = 120
enable_market_orders        = false
enable_fee_optimization     = true

enable_order_lifecycle         = true
order_max_age_minutes          = 5
order_refresh_interval_minutes = 2
min_orders_to_maintain         = 8

# 🔥 CRITICAL: Tuned Fee Filter (Battle Royale Learning)
min_profit_threshold_pct = 0.25
max_slippage_pct         = 1.5

enable_price_bounds = false

# 🆕 TUNED REGIME GATE (Battle Royale + 150%)
enable_regime_gate      = true
min_volatility_to_trade = 0.05
pause_in_very_low_vol   = true

enable_smart_position_sizing = true

[strategies]
active         = ["grid"]
consensus_mode = "single"
enable_multi_timeframe      = false
require_timeframe_alignment = false

[strategies.grid]
enabled        = true
weight         = 1.0
min_confidence = 0.5

[strategies.momentum]
enabled         = false
weight          = 0.8
min_confidence  = 0.6
lookback_period = 20
threshold       = 0.02

[strategies.mean_reversion]
enabled            = false
weight             = 0.7
min_confidence     = 0.6
sma_period         = 20
std_dev_multiplier = 2.0

[strategies.rsi]
enabled              = false
weight               = 0.9
min_confidence       = 0.7
period               = 14
oversold_threshold   = 30.0
overbought_threshold = 70.0

[risk]
max_position_size_pct        = 30.0
max_drawdown_pct             = 5.0
stop_loss_pct                = 3.0
take_profit_pct              = 8.0
enable_circuit_breaker       = true
circuit_breaker_threshold_pct = 4.0
circuit_breaker_cooldown_secs = 300

# ═══════════════════════════════════════════════════════════════════════════
# 🪐 JUPITER API CONFIGURATION (REQUIRED FOR LIVE TRADING)
# ═══════════════════════════════════════════════════════════════════════════
[jupiter]
# Jupiter API Key (REQUIRED)
# Get free at: https://portal.jup.ag
# ⚠️  Note: This key is visible in repo. Rotate if needed post-testing.
api_key = "50c73ffd-2350-45f3-90da-50caa00cd24c"

# API endpoint (current production)
api_endpoint = "https://api.jup.ag"

# Slippage tolerance in basis points (50 = 0.5%)
# Tighter than execution.max_slippage_bps for normal conditions
default_slippage_bps = 50

# Priority fee configuration
# Matches execution.priority_fee_microlamports for consistency
priority_fee_max_lamports = 100000  # 0.0001 SOL max priority fee
priority_level = "high"  # Options: none | low | medium | high | veryHigh

# ═══════════════════════════════════════════════════════════════════════════

[pyth]
http_endpoint    = "https://hermes.pyth.network"
feed_ids         = ["0xef0d8b6fda2ceba41da15d4095d1da392a0d2f8ed0c6c7bc0f4cfac8c280b56d"]
update_interval_ms = 500

[logging]
level              = "info"
file_path          = "logs/mainnet/mainnet-sol-usdc-v1.log"
enable_file_logging = true

[metrics]
enable_metrics = true
stats_interval = 50

[performance]
cycle_interval_ms  = 100
startup_delay_ms   = 2000
request_timeout_ms = 5000
