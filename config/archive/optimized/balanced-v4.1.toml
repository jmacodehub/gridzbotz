# ═══════════════════════════════════════════════════════════════════════════
# ⚖️ BALANCED V4.1 - ALL-WEATHER GRID BOT
# ═══════════════════════════════════════════════════════════════════════════
#
# STRATEGY: Medium-frequency balanced approach with V2.5 enhancements
# RISK LEVEL: MEDIUM (Balanced risk-reward)
# TARGET: All market conditions with consistent profitability
# EXPECTED RETURN: 8-15% monthly with lower drawdown
#
# V2.5 ENHANCEMENTS:
# ✅ Market Regime Gate - Balanced pause thresholds
# ✅ Smart Fee Filtering - Moderate fee tolerance
# ✅ Order Lifecycle - Standard refresh intervals
# ✅ Dynamic Spacing - Moderate volatility adjustment
#
# February 13, 2026 - Proven Performer! ⚖️
# ═══════════════════════════════════════════════════════════════════════════

[bot]
name = "Balanced-V4.1"
version = "4.1"
mode = "simulation"

# ───────────────────────────────────────────────────────────────────────────
# 🎯 TRADING PAIR
# ───────────────────────────────────────────────────────────────────────────
[pair]
base = "SOL"
quote = "USDC"
min_price = 70.0
max_price = 220.0

# ───────────────────────────────────────────────────────────────────────────
# 📊 GRID CONFIGURATION - BALANCED
# ───────────────────────────────────────────────────────────────────────────
[grid]
levels = 10                    # ⚖️ MEDIUM: Balanced trade frequency
spacing_percent = 1.5          # ⚖️ MODERATE: 1.5% between levels
position_size_usdc = 150.0     # $150 per level
min_order_size_usdc = 15.0

# Dynamic spacing (V2.5)
enable_dynamic_spacing = true
volatility_multiplier = 1.3    # Moderate adjustment
volatility_lookback = 24

# Grid rebalancing
auto_rebalance = true
rebalance_threshold = 7.0      # More conservative
rebalance_cooldown_minutes = 60

# ───────────────────────────────────────────────────────────────────────────
# 🛡️ RISK MANAGEMENT - BALANCED
# ───────────────────────────────────────────────────────────────────────────
[risk]
max_position_usdc = 1500.0     # Lower total exposure
max_drawdown_percent = 12.0    # Tighter drawdown limit
stop_loss_percent = 6.0        # Individual position stop at -6%

max_open_orders = 20
max_daily_trades = 100

emergency_stop_drawdown = 18.0

# ───────────────────────────────────────────────────────────────────────────
# 🚦 V2.5 MARKET REGIME GATE - BALANCED
# ───────────────────────────────────────────────────────────────────────────
[regime_gate]
enabled = true

# Balanced volatility thresholds
volatility_threshold_low = 0.012   # 1.2%
volatility_threshold_high = 0.06   # 6%

min_trend_strength = 0.35
trend_lookback_periods = 48

check_interval_seconds = 300

auto_resume = true
resume_cooldown_minutes = 20

# Balanced regime selection
allow_trending = true
allow_ranging = true
allow_volatile = false         # Skip extreme volatility
allow_choppy = false           # Skip choppy markets

# ───────────────────────────────────────────────────────────────────────────
# 💰 V2.5 SMART FEE FILTERING - BALANCED
# ───────────────────────────────────────────────────────────────────────────
[fee_filtering]
enabled = true

max_fee_percent = 0.12         # Lower base limit

# Balanced fee limits
trending_max_fee = 0.15
ranging_max_fee = 0.10
volatile_max_fee = 0.18
choppy_max_fee = 0.08

include_priority_fees = true
priority_fee_lamports = 5000   # Lower priority fee

log_rejected_trades = true
rejection_cooldown_seconds = 60

# ───────────────────────────────────────────────────────────────────────────
# 🔄 V2.5 ORDER LIFECYCLE - BALANCED
# ───────────────────────────────────────────────────────────────────────────
[order_lifecycle]
enabled = true

max_order_age_minutes = 45     # Longer refresh interval
stale_check_interval_seconds = 300

auto_refresh_stale = true
refresh_price_deviation = 0.8  # Less aggressive refresh

auto_cancel_stale = true
cancel_age_minutes = 90

track_fill_rates = true
min_fill_rate_percent = 8.0

# ───────────────────────────────────────────────────────────────────────────
# 📈 INDICATORS & SIGNALS
# ───────────────────────────────────────────────────────────────────────────
[indicators]

[indicators.atr]
period = 14
multiplier = 2.0

[indicators.rsi]
period = 14
overbought = 70
oversold = 30

[indicators.bollinger]
period = 20
std_dev = 2.0

[indicators.macd]
fast_period = 12
slow_period = 26
signal_period = 9

# ───────────────────────────────────────────────────────────────────────────
# 🌐 RPC & DATA FEEDS
# ───────────────────────────────────────────────────────────────────────────
[rpc]
primary = "https://api.mainnet-beta.solana.com"
fallback = ["https://solana-api.projectserum.com"]
timeout_seconds = 15
max_retries = 3

[price_feed]
provider = "pyth"
update_interval_ms = 2000      # 2 second updates (balanced)
redundancy = true
fallback_providers = ["chainlink", "switchboard"]

# ───────────────────────────────────────────────────────────────────────────
# 📊 MONITORING & LOGGING
# ───────────────────────────────────────────────────────────────────────────
[monitoring]
log_level = "info"
log_trades = true
log_regime_changes = true
log_fee_rejections = true

track_pnl = true
track_fill_rate = true
track_regime_stats = true

alert_on_drawdown = true
alert_threshold = 8.0

export_trades_csv = true
export_interval_minutes = 60

# ───────────────────────────────────────────────────────────────────────────
# 🎯 OPTIMIZATION PARAMS
# ───────────────────────────────────────────────────────────────────────────
[optimization]
auto_tune = false
learning_rate = 0.01
adaptation_window = 1000

# ═══════════════════════════════════════════════════════════════════════════
# END BALANCED V4.1 CONFIG
# ═══════════════════════════════════════════════════════════════════════════
